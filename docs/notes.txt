
if empty get energy
	if a container exists get from there 
	if no container get from storage
	if no storage then harvest


if task
	run task
	if empty after task then clear task


if (not empty AND no task) do Decision tree:

	If Room energy is not full then store - task : store






	if nothing else then upgrade


